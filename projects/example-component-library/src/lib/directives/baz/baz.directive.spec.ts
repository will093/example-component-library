import { BazDirective } from './baz.directive';

describe('BazDirective', () => {
  it('should create an instance', () => {
    const directive = new BazDirective();
    expect(directive).toBeTruthy();
  });
});
